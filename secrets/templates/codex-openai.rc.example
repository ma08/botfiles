# OpenAI API for Codex
export OPENAI_API_KEY="replace_with_your_openai_api_key"

# Deep Research direct OpenAI usage is disabled by default in the bundled
# runner. Prefer Azure OpenAI deep-research settings in codex-azure.rc; opt in
# only when direct OpenAI billing is intentional.
# export OPENAI_DEEP_RESEARCH_ALLOW_DIRECT="1"
