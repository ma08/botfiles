# Shared machine identity used across notifications and task-status workflows.
export SYSTEM_NAME="replace_with_machine_name"

# SSH host alias other machines can use to reach this machine.
# Screenshot local-agent payloads use this as their compact `machine` value.
export BOT_MACHINE_SSH_ALIAS="replace_with_ssh_alias"
