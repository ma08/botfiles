interface:
  display_name: "Mac UI Worker"
  short_description: "Route Mac Browser and Computer Use jobs"
  default_prompt: "Use $mac-ui-worker to route this bounded Mac UI job through the declared in-app Browser or Computer Use surface and return a sanitized receipt."
