# Agent Instructions

## Environment Setup

```bash
# Example: Activate conda environment
# conda activate myenv

# Example: Set PATH
# export PATH="/path/to/bin:$PATH"
```

## Build Commands

```bash
# Example: Build the project
# npm run build
# python setup.py build
```

## Test Commands

```bash
# Run specific test file (preferred - faster feedback)
# pytest tests/test_feature.py -v

# Run tests with coverage
# pytest tests/ --cov=src/
```

## Type Checking

```bash
# Example: Python type checking
# mypy src/ --ignore-missing-imports

# Example: TypeScript
# npx tsc --noEmit
```

## Notes

- Ralph updates this file when learning new commands
- Keep commands specific and tested
- Include the actual paths used in this project
