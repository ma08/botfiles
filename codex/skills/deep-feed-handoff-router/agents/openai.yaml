interface:
  display_name: "Deep Feed Handoff Router"
  short_description: "Safely stop an obsolete Deep Feed router invocation"
  default_prompt: "Use $deep-feed-handoff-router to inspect this legacy router invocation without dispatching it."
