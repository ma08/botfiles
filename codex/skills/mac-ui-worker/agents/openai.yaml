interface:
  display_name: "Mac UI Worker"
  short_description: "Route Mac UI jobs with direct collaboration"
  default_prompt: "Use $mac-ui-worker to send one complete bounded Mac UI job, collaborate with me directly in the worker, and reserve source callbacks for declared dependencies and the sanitized receipt."
